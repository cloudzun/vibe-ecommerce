# Phase 4 Implementation Plan — User Authentication

**Date**: 2026-03-05  
**Estimated total**: ~90 minutes

---

## Task 1: 安装依赖 + 数据库迁移（5 min）
- Input: server/package.json, server/db.js
- Output: 新增 users 表，orders 表加 user_id 列，安装 jsonwebtoken/bcryptjs/express-rate-limit
- Verification: `node --check server/db.js` && sqlite3 检查表结构

## Task 2: JWT 中间件（10 min）
- Input: 无
- Output: server/middleware/auth.js — verifyToken(req, res, next)
- Verification: `node --check server/middleware/auth.js`

## Task 3: Auth 路由（20 min）
- Input: server/middleware/auth.js, server/db.js
- Output: server/routes/auth.js — POST /register, /login, /refresh, /logout
- Verification: curl 测试 register + login 返回 token

## Task 4: Users 路由（10 min）
- Input: server/middleware/auth.js
- Output: server/routes/users.js — GET /api/users/me/orders
- Verification: curl 带 token 测试返回订单列表

## Task 5: pm2 ecosystem 配置（5 min）
- Input: JWT_SECRET
- Output: server/ecosystem.config.js — 注入环境变量，重启服务
- Verification: `curl https://shop-api.huaqloud.com/health`

## Task 6: 前端 auth 工具层（10 min）
- Input: 无
- Output: js/auth.js — getToken/setToken/clearToken/isLoggedIn/getUser
- Verification: `node --check js/auth.js`

## Task 7: 登录页 + 注册页（15 min）
- Input: js/auth.js
- Output: js/components/login.js (#login), js/components/register.js (#register)
- Verification: `node --check` 两个文件

## Task 8: 账户页（10 min）
- Input: js/auth.js
- Output: js/components/account.js (#account) — 显示订单历史
- Verification: `node --check js/components/account.js`

## Task 9: Header + Checkout 更新（10 min）
- Input: js/auth.js
- Output: header.js 显示登录状态，checkout.js 登录时预填 name/email + 传 token
- Verification: `node --check` 两个文件

## Task 10: index.html 注册新脚本（5 min）
- Input: 新增的 js 文件列表
- Output: index.html 加入 auth.js + 三个新组件的 script 标签
- Verification: 浏览器打开无 console error
